Required:
- `agent_name_v2.png` (sample of your own kill-feed row UI in top-right)
- fallback accepted: `agent_name.png`

Optional:
- kill-feed symbol templates (skull / weapon markers)

Examples:
- `kill_skull.png`
- `weapon_vandal.png`
- `weapon_phantom.png`

Kill counting uses UI template match + OCR username detection + non-red background filter.
